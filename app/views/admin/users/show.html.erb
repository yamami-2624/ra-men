<div class="container">
    <div class="row">
        <div class="col-xs-10 col-xs-offset-1">
			<h2 class="section-ttl">ユーザー詳細</h2>
			<div class="flex">
				<table class="table">
					<thead>
						<tr>
							<th></th>
							<th></th>
						</tr>
					</thead>
					<tbody>
						<tr>
							<td>名前</td>
							<td><%= @user.name %></td>
						</tr>
						<tr>
							<td>メールアドレス</td>
							<td><%= @user.email %></td>
						</tr>
					</tbody>
				</table>
				<%= render 'admin/reviews/index', reviews: @user.reviews, user: @user %>
			</div>
		</div>
	</div>
</div>