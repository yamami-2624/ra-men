<div class="container">
    <div class="row">
        <div class="col-xs-6 col-xs-offset-1">
        	<h1>ラーメンを食べに行こう！！</h1>
        </div>
        <div class="col-xs-10 col-xs-offset-1">
        	<table class="table">
        		<thead>
        		</thead>
        		<tbody>
        			<tr>
        				<td>
							<%= form_with(url: search_shop_path, local: true, method: :get, class: "search-form") do |form| %>
								<%= form.text_field :keyword, placeholder: "店を検索する", class: "search-input" %>
								<%= form.submit "検索", class: "search-btn" %>
							<% end %>
						</td>
						<td>
							<%= form_with(url: search_raamen_path, local: true, method: :get, class: "search-form") do |form| %>
								<%= form.text_field :keyword, placeholder: "メニューから検索する", class: "search-input" %>
								<%= form.submit "検索", class: "search-btn" %>
							<% end %>
						</td>
						<td>
							<h4>現在地から選ぶ</h4>
						</td>
					</tr>
				</tbody>
			</table>
		</div>
	</div>
</div>
